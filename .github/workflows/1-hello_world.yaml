name: 1-Hello-Darkness Github actions

on: #workflow triggering 
 workflow-dispatch:
 #schedule:
  #- cron: '30 15 * * *'
  # push: #git push
 # brancn:
  #- main

jobs:
 hello:
  runs-on: ubuntu-latest
  steps:
  - name: Hello Baby
    run: echo "hello"  